# poller.py reads the environment only (no .env file loading).
# Locally: export these in your shell, or use a tool like direnv.

# ChargePoint (same names as GitHub Actions secrets)
CHARGEPOINT_USER=you@example.com
CHARGEPOINT_PASS=

# Strongly recommended for GitHub Actions / SSO / 2FA: ChargePoint often returns
# a Datadome captcha on password login from automation IPs. Log in with a normal
# browser, copy the coulomb_sess cookie value, and set it here or as repo secret
# CHARGEPOINT_SESSION_TOKEN. The poller persists rotated coulomb_sess values
# between runs (~/.chargepoint_session_token by default); the env/secret value
# is only used to bootstrap or recover when that saved token is missing/expired.
# CHARGEPOINT_SESSION_TOKEN=
# Optional override for where the rotated token is stored on the runner:
# CHARGEPOINT_TOKEN_STATE_PATH=~/.chargepoint_session_token

CHARGEPOINT_STATION_IDS=12345678,87654321

# Default co-op charging window (minutes) written to /stations/charging_limit_minutes each poll.
# CHARGING_LIMIT_MINUTES=120

# Firebase Realtime Database URL
FIREBASE_DATABASE_URL=https://YOUR_PROJECT_ID-default-rtdb.firebaseio.com/

# Local: path to service account JSON. CI: use FIREBASE_KEY secret (full JSON string) instead.
# FIREBASE_CREDENTIALS_PATH=firebase-key.json
