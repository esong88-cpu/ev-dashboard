{
  "rules": {
    "stations": {
      ".read": true,
      ".write": false
    },
    "slot_extensions": {
      ".read": true,
      "$slot": {
        ".write": true,
        ".validate": "newData.hasChildren(['until_ms']) && newData.child('until_ms').isNumber() && newData.child('until_ms').val() > now - 600000 && newData.child('until_ms').val() <= now + 86400000 && (!newData.child('by').exists() || (newData.child('by').isString() && newData.child('by').val().length <= 80)) && (!newData.child('extra_minutes').exists() || (newData.child('extra_minutes').isNumber() && newData.child('extra_minutes').val() >= 15 && newData.child('extra_minutes').val() <= 480)) && (!newData.child('at').exists() || newData.child('at').isString())"
      }
    },
    "slot_resets": {
      ".read": true,
      "$slot": {
        ".write": true,
        ".validate": "newData.hasChildren(['at_ms']) && newData.child('at_ms').isNumber() && newData.child('at_ms').val() > now - 600000 && newData.child('at_ms').val() <= now + 300000 && (!newData.child('at').exists() || newData.child('at').isString())"
      }
    }
  }
}
