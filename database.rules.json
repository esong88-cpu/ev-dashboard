{
  "rules": {
    "stations": {
      ".read": true,
      ".write": false
    },
    "slot_extensions": {
      ".read": true,
      "$slot": {
        ".write": true,
        ".validate": "newData.hasChildren(['until_ms', 'extra_minutes']) && newData.child('until_ms').isNumber() && newData.child('until_ms').val() > now && newData.child('until_ms').val() <= now + 36000000 + 300000 && newData.child('extra_minutes').isNumber() && newData.child('extra_minutes').val() >= 15 && newData.child('extra_minutes').val() <= 480 && (!newData.child('by').exists() || newData.child('by').isString()) && (!newData.child('at').exists() || newData.child('at').isString())"
      }
    },
    "slot_resets": {
      ".read": true,
      "$slot": {
        ".write": true,
        ".validate": "newData.hasChild('at_ms') && newData.child('at_ms').isNumber() && newData.child('at_ms').val() >= now - 300000 && newData.child('at_ms').val() <= now + 300000 && (!newData.child('at').exists() || newData.child('at').isString())"
      }
    }
  }
}
